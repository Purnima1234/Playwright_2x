# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: 06_Multiple_Element_\252_DynamicWebtables.spec.ts >> verify the webtables values
- Location: tests\06_Multiple_Element_\252_DynamicWebtables.spec.ts:3:5

# Error details

```
Error: page.goto: net::ERR_NAME_NOT_RESOLVED at https://awsomeqa.com/webtable.html
Call log:
  - navigating to "https://awsomeqa.com/webtable.html", waiting until "load"

```

# Test source

```ts
  1  | import { test, expect } from '@playwright/test';
  2  | 
  3  | test("verify the webtables values", async ({page})=> {
> 4  | await page.goto("https://awsomeqa.com/webtable.html");
     |            ^ Error: page.goto: net::ERR_NAME_NOT_RESOLVED at https://awsomeqa.com/webtable.html
  5  | const table= await page.locator('//table[@id="customers"]');
  6  | const rows= await table.locator('tr');
  7  | 
  8  | const rowcount= await rows.count();
  9  | 
  10 | for(let i=0; i<rowcount; i++) {
  11 |     const row= await rows.nth(i).textContent();
  12 |     console.log(row);
  13 | 
  14 |     let cells= await rows.nth(i).locator('td');
  15 |     let cellcount= await cells.count();
  16 |     for (let j=0; j<cellcount; j++){
  17 |         const cell= await cells.nth(j).textContent();
  18 |         console.log(cell);
  19 |     }
  20 | 
  21 | }
  22 | });
  23 | 
```